# /usr/lib/cgi-bin/app/config/.htaccess

# Deny all access
Require all denied

# Remove CGI execution
Options -ExecCGI
RemoveHandler .py .cgi .pl

# Block specific file types
<FilesMatch "\.(py|pyc|db|sqlite|conf|ini|json)$">
    Require all denied
</FilesMatch>

# No directory listing
Options -Indexes